# Railway ignore file
# Note: Dockerfile in root is now used for Railway deployment

# Ignore docker-compose files
docker-compose.yml
docker-compose.yaml

# Ignore backup Dockerfile
backend/Dockerfile.backup
