# TrendForge Architecture v2.0.1

**版本**: 2.0.1-quickfix  
**日期**: 2026-01-13

---

## 🏗️ 系统架构

### 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        Frontend (Next.js)                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Home Page  │  │ Analysis Page│  │  API Routes   │     │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘     │
│         │                  │                  │             │
│         └──────────────────┼──────────────────┘           │
│                            │                                │
└────────────────────────────┼────────────────────────────────┘
                             │ HTTP/REST API
                             │
┌────────────────────────────┼────────────────────────────────┐
│                   Backend (FastAPI)                         │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              API Endpoints (app_v2.py)              │  │
│  │  - /api/v2/full-analysis                            │  │
│  │  - /api/v2/collect-social-trends                     │  │
│  │  - /health                                           │  │
│  └───────────────────┬──────────────────────────────────┘  │
│                      │                                       │
│  ┌───────────────────┼──────────────────────────────────┐ │
│  │              Service Layer                            │ │
│  │  ┌────────────────┐  ┌────────────────┐            │ │
│  │  │ YouTube        │  │ Social Media   │            │ │
│  │  │ Analyzer       │  │ Collector      │            │ │
│  │  │                │  │                │            │ │
│  │  │ - Topics       │  │ - Twitter      │            │ │
│  │  │ - Style        │  │ - Reddit       │            │ │
│  │  │ - Audience     │  │ - Google       │            │ │
│  │  │ - Performers   │  │ - SerpAPI ⭐   │            │ │
│  │  └────────────────┘  └────────────────┘            │ │
│  │                                                     │ │
│  │  ┌────────────────┐  ┌────────────────┐            │ │
│  │  │ Recommendation │  │ Backtest       │            │ │
│  │  │ Engine         │  │ Analyzer       │            │ │
│  │  │                │  │                │            │ │
│  │  │ - Match Score  │  │ - Accuracy     │            │ │
│  │  │ - Titles       │  │ - Outliers     │            │ │
│  │  └────────────────┘  │ - ML Models    │            │ │
│  │                      └────────────────┘            │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                             │
                ┌────────────┼────────────┐
                │            │            │
        ┌───────▼──────┐ ┌──▼──────┐ ┌──▼──────┐
        │  PostgreSQL  │ │  Redis  │ │External │
        │  (Prisma)    │ │ (Cache) │ │  APIs   │
        └──────────────┘ └─────────┘ └─────────┘
```

---

## 📁 目录结构

```
TrendForge/
├── backend/                          # 后端服务
│   ├── app_v2.py                    # FastAPI 主应用 (v2.0.1)
│   ├── requirements_v2.txt          # Python 依赖
│   ├── .env                         # 环境变量
│   ├── Dockerfile                   # 容器配置
│   └── services/                    # 核心服务模块
│       ├── enhanced_youtube_analyzer.py    # 内容分析器
│       ├── enhanced_social_collector.py    # 社交趋势收集器
│       │   ├── EnhancedTwitterCollector
│       │   ├── EnhancedRedditCollector
│       │   ├── EnhancedGoogleTrendsCollector
│       │   ├── EnhancedSerpAPICollector ⭐  # 新增
│       │   └── CrossPlatformSignalAggregator
│       ├── intelligent_recommender.py      # 推荐引擎
│       ├── backtest_analyzer.py          # 回测分析器
│       └── ml_predictor.py                # ML 模型预测器
│
├── frontend/                         # 前端应用
│   ├── app/                          # Next.js App Router
│   │   ├── page.tsx                 # 首页
│   │   ├── analysis/[channelId]/page.tsx  # 分析结果页面
│   │   └── api/                     # API 路由
│   │       ├── analyze/route.ts     # 分析 API
│   │       └── analysis/[channelId]/route.ts  # 获取结果
│   ├── components/                   # React 组件
│   │   └── TrendPredictionChart.tsx
│   ├── lib/                         # 工具函数
│   │   ├── prisma.ts                # Prisma 客户端
│   │   └── youtube-public.ts        # YouTube API
│   ├── prisma/                      # 数据库 Schema
│   │   └── schema.prisma
│   ├── package.json
│   └── Dockerfile
│
├── vercel.json                      # Vercel 部署配置
├── railway.json                     # Railway 部署配置
├── docker-compose.yml               # Docker 部署配置
│
└── docs/                            # 文档
    ├── VERSION_2.0.1_SERPAPI.md    # 版本说明
    ├── ARCHITECTURE_V2.0.1.md      # 架构文档（本文件）
    ├── DEPLOYMENT_GUIDE.md          # 部署指南
    └── ALGORITHM_DOCUMENTATION.md   # 算法文档
```

---

## 🔄 数据流

### 完整分析流程

```
1. 用户输入频道标识符
   ↓
2. 前端 API (/api/analyze)
   ├── 检查缓存（当天数据）
   ├── 调用 YouTube API 获取频道数据
   └── 调用后端 /api/v2/full-analysis
   ↓
3. 后端分析管道
   ├── Step 1: 深度频道分析 (enhanced_youtube_analyzer)
   │   ├── 提取主题 (TF-IDF + NER + KeyBERT)
   │   ├── 分析高表现视频
   │   ├── 识别内容风格
   │   └── 分析目标受众
   │
   ├── Step 2: 社交趋势收集 (enhanced_social_collector)
   │   ├── Twitter API (15s 超时)
   │   ├── Reddit API (15s 超时)
   │   ├── Google Trends (15s 超时)
   │   └── SerpAPI ⭐ (15s 超时，替代方案)
   │   └── 聚合信号 (CrossPlatformSignalAggregator)
   │
   ├── Step 3: 生成推荐 (intelligent_recommender)
   │   ├── 计算匹配分数
   │   ├── 生成内容角度
   │   └── 评估紧急程度
   │
   ├── Step 4: 回测分析 (backtest_analyzer) ⭐ 优化
   │   ├── 选择视频（至少50个，如果可用）
   │   ├── 训练 ML 模型（如果 >= 20 个视频）
   │   ├── 预测每个视频的播放量
   │   ├── 计算准确度指标
   │   └── 识别优秀表现视频
   │
   └── Step 5: AI 标题生成 (title_engine)
       └── 为每个推荐生成3个标题变体
   ↓
4. 保存到数据库
   ├── Channel 记录
   ├── Trend 记录
   └── ChannelTrend 记录（推荐数据）
   ↓
5. 返回分析结果
   ↓
6. 前端显示结果页面
```

---

## 🧩 核心模块

### 1. Enhanced YouTube Analyzer

**文件**: `backend/services/enhanced_youtube_analyzer.py`

**功能**:
- 主题提取（TF-IDF + NER + KeyBERT）
- 高表现视频分析
- 内容风格识别
- 目标受众分析

**主要类**:
- `EnhancedContentAnalyzer`
- `EnhancedAudienceAnalyzer`

### 2. Enhanced Social Collector

**文件**: `backend/services/enhanced_social_collector.py`

**功能**:
- 多平台趋势收集
- 信号聚合和评分
- 缓存管理（Redis + 内存）

**主要类**:
- `EnhancedTwitterCollector`
- `EnhancedRedditCollector`
- `EnhancedGoogleTrendsCollector`
- `EnhancedSerpAPICollector` ⭐ **新增**
- `CrossPlatformSignalAggregator`
- `CacheManager`

### 3. Intelligent Recommender

**文件**: `backend/services/intelligent_recommender.py`

**功能**:
- 推荐生成
- 匹配分数计算
- 内容角度建议

**主要类**:
- `TopicRecommendationEngine`
- `TitleGenerationEngine`

### 4. Backtest Analyzer

**文件**: `backend/services/backtest_analyzer.py`

**功能**:
- 预测算法回测
- 准确度评估
- 优秀表现视频识别

**主要类**:
- `BacktestAnalyzer`

**优化**:
- 至少处理50个视频（如果可用）
- 同步函数（修复异步问题）
- 超时保护（60秒 for 50+ videos）

### 5. ML Predictor

**文件**: `backend/services/ml_predictor.py`

**功能**:
- ML 模型训练（Random Forest, XGBoost, LightGBM）
- 特征提取
- 播放量预测

**主要类**:
- `MLPredictor`

---

## 💾 数据模型

### 数据库 Schema (Prisma)

```prisma
model User {
  id        String   @id @default(uuid())
  email     String   @unique
  channels  Channel[]
}

model Channel {
  id          String        @id @default(uuid())
  channelId   String        @unique
  title       String
  fingerprint Json          // 存储完整分析结果
  trends      ChannelTrend[]
  lastAnalyzed DateTime?
}

model Trend {
  id      String   @id @default(uuid())
  keyword String   @unique
  channelTrends ChannelTrend[]
}

model ChannelTrend {
  id                String   @id @default(uuid())
  channelId         String
  trendId           String
  matchScore        Float
  recommendationData Json?   // 存储完整推荐数据
  channel           Channel  @relation(...)
  trend             Trend    @relation(...)
}
```

### 数据存储结构

**Channel.fingerprint.v2_analysis**:
```json
{
  "topics": [...],
  "content_style": {...},
  "target_audience": {...},
  "high_performers": {...},
  "social_trends": {...},
  "backtest": {...},              // 回测结果
  "backtest_status": {...},       // 回测状态
  "total_videos_analyzed": 50
}
```

**ChannelTrend.recommendationData**:
```json
{
  "keyword": "...",
  "match_score": 75.5,
  "viral_potential": 80.2,
  "predicted_performance": {...},
  "suggested_titles": [...],
  "reasoning": "..."
}
```

---

## 🔐 安全配置

### API 密钥管理
- 所有 API 密钥存储在环境变量中
- 前端不直接访问敏感 API
- 所有 API 调用通过后端代理

### CORS 配置
```python
allowed_origins = [
    "http://localhost:3000",
    "https://*.vercel.app",
    "https://your-app.vercel.app"
]
```

### 错误处理
- 不向客户端暴露敏感错误详情
- API 限流时使用 fallback 数据
- 关键错误使用 HTTPException

---

## 📊 性能优化

### 缓存策略

1. **数据库缓存** (前端)
   - 当天分析的频道直接返回缓存
   - 避免重复 API 调用

2. **Redis 缓存** (后端)
   - 社交趋势数据缓存 1 小时
   - 减少外部 API 调用

3. **内存缓存** (后端)
   - Redis 不可用时的 fallback
   - 临时数据存储

### 超时保护

- 频道分析: 45秒
- 社交趋势收集: 15秒/平台
- 回测分析: 30秒（<50视频）或 60秒（>=50视频）
- 标题生成: 20秒

### 并行处理

- 社交平台数据并行收集
- 每个平台独立超时
- 使用 `asyncio.gather` 并行执行

---

## 🧪 测试和调试

### 健康检查

```bash
curl http://localhost:8000/health
```

**响应示例**:
```json
{
  "status": "healthy",
  "services": {
    "twitter": true,
    "reddit": false,
    "google_trends": true,
    "serpapi": true,
    "cache": true
  }
}
```

### 日志文件

- 后端: `/tmp/trendforge-backend.log`
- 前端: `/tmp/trendforge-frontend.log`

### 调试脚本

- `backend/diagnose.py` - 性能诊断
- `backend/performance_diagnostic.py` - 性能分析

---

## 🚀 部署架构

### 生产环境

```
┌─────────────┐         ┌─────────────┐
│   Vercel    │         │   Railway   │
│  (Frontend) │────────▶│  (Backend)  │
└─────────────┘         └──────┬──────┘
                               │
                    ┌──────────┼──────────┐
                    │          │          │
              ┌─────▼───┐ ┌───▼────┐ ┌──▼────┐
              │PostgreSQL│ │ Redis  │ │ APIs  │
              └──────────┘ └────────┘ └───────┘
```

### 本地开发

```bash
# 后端
cd backend
source venv/bin/activate
python app_v2.py

# 前端
cd frontend
pnpm dev
```

---

## 📝 代码规范

### Python 后端
- 类名: `PascalCase`
- 函数名: `snake_case`
- 私有方法: `_leading_underscore`
- 类型提示: 使用 `typing` 模块

### TypeScript 前端
- 组件名: `PascalCase`
- 函数/变量: `camelCase`
- 接口/类型: `PascalCase`
- Props 接口: `Props` 后缀

---

## 🔄 版本控制

### Git 工作流

```bash
# 提交当前版本
git add .
git commit -m "feat: v2.0.1 - SerpAPI integration and backtest optimization"
git tag -a v2.0.1 -m "Version 2.0.1 with SerpAPI"
git push origin main --tags
```

### 版本标签

- `v2.0.1` - 当前版本（SerpAPI 集成）
- `v2.0.0` - MVP 2.0 基础版本

---

**最后更新**: 2026-01-13  
**维护者**: TrendForge 开发团队
