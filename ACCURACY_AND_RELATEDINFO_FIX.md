# 算法准确度和相关信息显示修复指南

**问题**:
1. 算法准确度与 localhost 相差巨大
2. "相关信息"话题信息并未展示（hashtags 和 subreddits）

## 🔍 问题分析

### 问题 1: 算法准确度差异

**可能原因**:
1. **数据量不同**: 生产环境可能使用了不同数量的视频进行回测
2. **ML 模型训练差异**: 如果数据量不足，ML 模型可能未启用或使用不同的模型
3. **随机种子**: 如果使用了随机数，不同环境的随机种子可能导致结果不同
4. **数据质量**: 生产环境的视频数据可能与 localhost 不同

**回测逻辑**:
- 使用最近的 50 个视频（如果可用）
- 如果视频数 >= 20，启用 ML 模型
- 如果视频数 < 20，使用传统预测方法
- 测试集比例：30-40%（至少 10 个样本）

### 问题 2: 相关信息未显示

**根本原因**:
1. **模拟社交趋势数据**: 当没有真实社交媒体数据时，后端生成模拟数据，但 `twitter_hashtags` 和 `reddit_subreddits` 被设置为空数组
2. **数据传递**: 即使后端生成了数据，前端可能没有正确读取

**数据流**:
```
后端生成推荐 → related_info.hashtags/subreddits → 保存到数据库 → 前端读取 → 显示
```

## 🔧 修复方案

### 修复 1: 生成模拟的 hashtags 和 subreddits

在 `backend/app_v2.py` 中，当生成模拟社交趋势数据时，添加智能的 hashtags 和 subreddits 生成逻辑：

```python
# 生成模拟的 hashtags（基于主题关键词）
topic_words = topic.lower().split()
mock_hashtags = []
for word in topic_words[:3]:
    if len(word) > 3:
        hashtag = word.replace(' ', '').title()
        mock_hashtags.append(hashtag)

# 生成模拟的 subreddits（基于主题类别）
mock_subreddits = []
topic_lower = topic.lower()
if any(word in topic_lower for word in ['trading', 'stock', 'invest']):
    mock_subreddits = ['stocks', 'investing', 'wallstreetbets']
elif any(word in topic_lower for word in ['tech', 'software', 'code']):
    mock_subreddits = ['technology', 'programming', 'software']
# ... 更多类别映射
```

### 修复 2: 确保数据正确传递

1. **后端**: 确保 `related_info` 包含 `hashtags` 和 `subreddits`
2. **前端**: 确保正确读取和显示这些数据

### 修复 3: 算法准确度一致性

1. **固定随机种子**: 如果使用了随机数，确保使用相同的随机种子
2. **统一数据量**: 确保使用相同数量的视频进行回测
3. **统一模型**: 确保使用相同的 ML 模型和参数

## 📋 验证步骤

### 验证相关信息显示

1. 重新分析频道
2. 打开推荐详情
3. 检查"相关信息"部分：
   - ✅ 相关标签（hashtags）应该显示
   - ✅ 热门 Subreddits 应该显示

### 验证算法准确度

1. 对比 localhost 和生产环境的回测结果
2. 检查：
   - 使用的视频数量是否相同
   - ML 模型是否启用
   - 测试集比例是否相同

---

**更新日期**: 2026-01-17
